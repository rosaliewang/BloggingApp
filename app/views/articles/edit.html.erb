<% provide(:title, 'Editing Blog') %>

<p>
  <% if @article.published? %>
      <span class="label label-success">Published</span>
  <% else %>
      <span class="label label-default">Draft</span>
  <% end %>
</p>

<div class="row">
  <div class="col-md-6 col-md-offset-3">
    <%= render 'form' %>
    <%= link_to 'Back', current_user %>
  </div>
</div>
